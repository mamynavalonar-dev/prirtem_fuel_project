# Server
PORT=3001
NODE_ENV=development

# Postgres
DATABASE_URL=postgres://postgres:123456@localhost:5432/prirtem_fuel

# Auth
JWT_SECRET=NavalonaDevfullstack2026

# Front URL (for reset link)
APP_URL=http://localhost:5173

# Optional SMTP (if empty: reset link is logged)
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
SMTP_FROM=no-reply@prirtem.local
