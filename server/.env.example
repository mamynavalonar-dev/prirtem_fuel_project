# Server
PORT=3001
NODE_ENV=development

# Postgres
DATABASE_URL=postgres://postgres:postgres@localhost:5432/prirtem_fuel

# Auth
JWT_SECRET=change_me_super_secret

# Front URL (for reset link)
APP_URL=http://localhost:5173

# Optional SMTP (if empty: reset link is logged)
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
SMTP_FROM=no-reply@prirtem.local
